# Atelier : Audit SQL et Logs dans Microsoft Fabric Warehouse – Voir précisément qui a fait quoi

Vous allez construire un atelier **Audit SQL** centré uniquement sur le **Warehouse** Microsoft Fabric :

- Activation des SQL Audit Logs (avec solution détaillée pour le bouton Save)
- Tests reproductibles entre Admin et Viewer
- Création d’une vue personnalisée `vw_audit_logs`
- Analyse fine des commandes exécutées (qui a fait quoi, quand, quelle commande ?)

Les comptes pédagogiques utilisés dans l’atelier sont :

- `participant@contactemeraudeit.onmicrosoft.com` : **Admin** (peut tout activer, créer la vue et tout analyser)
- `test@contactemeraudeit.onmicrosoft.com` : **Viewer** (accès très restreint)

Le compte `dev@contactemeraudeit.onmicrosoft.com` n’est mentionné que comme **compte d’administration du formateur** pour exécuter les manipulations qui exigent un niveau de droit élevé.

**Résultat attendu** : à la fin de l’atelier, les apprenants savent répondre à la question **« Qui a exécuté quelle commande T-SQL, quand et avec quel résultat ? »** directement dans le Warehouse.

---

## Prérequis

- Un tenant **Microsoft Fabric** actif avec l’audit activé au niveau tenant (activé par défaut)
- Une capacité Fabric compatible avec Warehouse
- Les comptes `participant@contactemeraudeit.onmicrosoft.com` (Admin) et `test@contactemeraudeit.onmicrosoft.com` (Viewer)
- Un compte administrateur formateur (`dev@contactemeraudeit.onmicrosoft.com`)
- La possibilité d’ouvrir plusieurs sessions navigateur ou fenêtres privées

## Références officielles utiles

- **SQL Audit Logs in Fabric Data Warehouse** : [https://learn.microsoft.com/en-us/fabric/data-warehouse/sql-audit-logs](https://learn.microsoft.com/en-us/fabric/data-warehouse/sql-audit-logs)
- **Configure SQL Audit Logs** : [https://learn.microsoft.com/en-us/fabric/data-warehouse/configure-sql-audit-logs](https://learn.microsoft.com/en-us/fabric/data-warehouse/configure-sql-audit-logs)

---

## Partie 1 - Créer le workspace et le Warehouse de démonstration

### 1.1 - Créer le workspace `WS_AUDIT_1`

1. Ouvrir **app.fabric.microsoft.com**
2. Vérifier que vous êtes connecté avec `dev@contactemeraudeit.onmicrosoft.com`
3. Dans la barre latérale gauche, cliquer sur **Workspaces**
4. Cliquer sur **+ New workspace**
5. Dans le champ **Name**, saisir `WS_AUDIT_1`
6. Dans le champ **Description**, saisir `Atelier Audit SQL Warehouse - Qui a fait quoi`
7. Cliquer sur **Apply**

| Champ | Valeur |
| --- | --- |
| **Name** | `WS_AUDIT_1` |
| **Description** | `Atelier Audit SQL Warehouse` |
| **Compte créateur** | `dev@contactemeraudeit.onmicrosoft.com` |

**Ce que vous devez voir à l’écran** :  
Le workspace `WS_AUDIT_1` est créé.

### 1.2 - Ajouter les deux comptes de test

1. Dans `WS_AUDIT_1`, cliquer sur **Workspace settings**
2. Ouvrir **Manage access**
3. Cliquer sur **Add people or groups**
4. Ajouter `participant@contactemeraudeit.onmicrosoft.com` → rôle **Admin**
5. Ajouter `test@contactemeraudeit.onmicrosoft.com` → rôle **Viewer**
6. Cliquer sur **Add**

| Compte | Rôle |
| --- | --- |
| `dev@contactemeraudeit.onmicrosoft.com` | Admin |
| `participant@contactemeraudeit.onmicrosoft.com` | **Admin** |
| `test@contactemeraudeit.onmicrosoft.com` | **Viewer** |

**Ce que vous devez voir à l’écran** :  
Les trois comptes apparaissent dans **Manage access**.

### 1.3 - Créer le Warehouse de démonstration

1. Revenir dans le workspace `WS_AUDIT_1`
2. Cliquer sur **+ New item** → rechercher **Warehouse** → Nom : `WH_RetailCorpAuditDemo` → **Create**

**Ce que vous devez voir à l’écran** :  
Le Warehouse `WH_RetailCorpAuditDemo` est créé et ouvert.

---

## Partie 2 - Activer les SQL Audit Logs et préparer les données

### 2.1 - Activer les SQL Audit Logs (uniquement avec Admin) – Étapes ultra-détaillées

1. Ouvrir le Warehouse `WH_RetailCorpAuditDemo` avec `participant@contactemeraudeit.onmicrosoft.com`
2. Dans la **page du Warehouse** (éditeur SQL ouvert), cliquer sur l’icône **Settings** (engrenage ⚙️ en haut à droite)
3. Dans le panneau qui s’ouvre, dans le menu de gauche, cliquer sur **SQL audit logs**
4. Vous êtes maintenant sur la page **SQL audit logs (preview)**
5. Vérifier que le toggle **Save events to SQL audit logs** est sur **On** (vert)
6. **Si le bouton Save en bas de la page reste grisé/désactivé** (comportement fréquent en Preview) :
  - Dans la section **Log retention**, changez temporairement la valeur **Years** de 9 à **1**
  - Ou bien, dans **Events to record**, décochez la case **Select all**, attendez 1 seconde, puis recocher **Select all**
  - Ou encore, éteignez puis rallumez le toggle principal **Save events to SQL audit logs**
7. Le bouton **Save** doit maintenant devenir actif (plus grisé)
8. Cliquer sur **Save**

**Ce que vous devez voir à l’écran** :  
Le bouton Save devient cliquable, puis un message de confirmation apparaît. Le statut reste **On**.

> **Note importante** : L’interface Fabric désactive souvent le bouton Save tant qu’aucune modification n’est détectée. Une petite modification (même temporaire) est nécessaire pour l’activer.

### 2.2 - Créer les tables et charger des données de test

1. Dans le Warehouse, nouvelle requête SQL, coller et exécuter :

```sql
CREATE SCHEMA sales;
GO

CREATE TABLE sales.orders (
    OrderId INT NOT NULL,
    Region NVARCHAR(20) NOT NULL,
    Amount DECIMAL(18,2) NOT NULL
);
GO

INSERT INTO sales.orders VALUES 
(1, 'Nord', 1490.00),
(2, 'Sud', 720.00);
GO
```

**Ce que vous devez voir à l’écran** :  
Le schéma `sales` et la table `sales.orders` sont créés avec 2 lignes.

---

## Partie 3 - Tests « qui fait quoi » avec les deux comptes

**Avec `participant@contactemeraudeit.onmicrosoft.com` (Admin)** :

1. Exécuter :

```sql
INSERT INTO sales.orders VALUES (3, 'Est', 950.00);
GO
```

**Avec `test@contactemeraudeit.onmicrosoft.com` (Viewer)** :

1. Exécuter :

```sql
SELECT * FROM sales.orders;
GO
```

2. Essayer (doit échouer) :

```sql
UPDATE sales.orders SET Amount = 1000 WHERE OrderId = 1;
GO
```

---

## Partie 4 - Créer et utiliser la vue personnalisée `vw_audit_logs`

### 4.1 - Obtenir les IDs du workspace et du warehouse

1. Dans l’onglet du Warehouse, copier l’URL complète du navigateur
2. Extraire les deux GUID : `{workspaceId}` et `{warehouseId}`

### 4.2 - Créer la vue personnalisée `vw_audit_logs`

1. Avec `participant@contactemeraudeit.onmicrosoft.com`
2. Nouvelle requête SQL
3. Coller (remplacez les GUID) :

```sql
CREATE OR ALTER VIEW vw_audit_logs AS
SELECT 
    event_time,
    action_id,
    succeeded,
    server_principal_name AS utilisateur,
    statement AS commande_executee,
    session_id,
    client_ip
FROM sys.fn_get_audit_file_v2(
    'https://onelake.blob.fabric.microsoft.com/{workspaceId}/{warehouseId}/Audit/sqldbauditlogs/',
    DEFAULT, DEFAULT, DEFAULT, DEFAULT)
WHERE event_time > DATEADD(MINUTE, -60, GETUTCDATE());
GO
```

4. Exécuter

### 4.3 - Consulter la vue

```sql
SELECT * FROM vw_audit_logs
ORDER BY event_time DESC;
```

---

## Partie 5 - Matrice des observations réelles (Admin vs Viewer)

| Action réalisée | Compte exécutant | Visible dans vw_audit_logs | Commentaire pédagogique |
| --- | --- | --- | --- |
| Activation SQL Audit Logs | participant (Admin) | ✅   | Seul Admin peut activer |
| INSERT dans table | participant (Admin) | ✅   | Logué avec commande complète |
| SELECT sur table | test (Viewer) | ✅   | Logué même en lecture seule |
| UPDATE refusé | test (Viewer) | ✅   | Tentative refusée visible |

---

## Partie 6 - Synthèse finale

**Bonne pratique** : activer les SQL Audit Logs sur tous les Warehouses de production et créer la vue `vw_audit_logs` pour une consultation rapide.

---

## Nettoyage (optionnel)

```sql
DROP VIEW IF EXISTS vw_audit_logs;
DROP TABLE IF EXISTS sales.orders;
DROP SCHEMA IF EXISTS sales;
GO
```

- Désactiver les SQL Audit Logs dans les Settings du Warehouse
- Supprimer le workspace `WS_AUDIT_1` si terminé
