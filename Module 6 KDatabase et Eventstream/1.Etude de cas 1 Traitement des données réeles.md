# Atelier : Monitoring temps réel d'un parc éolien — Eventstream & KQL Database

Vous allez ingérer des données IoT simulées (capteurs de turbines éoliennes) dans **Microsoft Fabric** via **Eventstream**, les stocker dans une **KQL Database**, les transformer en temps réel, les analyser avec des requêtes **KQL**, les visualiser dans un **Real-Time Dashboard**, accélérer le dashboard avec une **Materialized View** et configurer des **alertes automatiques** avec **Activator**.

Résultat attendu : un pipeline temps réel complet qui supervise 5 turbines éoliennes, met en évidence les anomalies (surchauffe, vibrations, sous-performance), enrichit les événements dans le flux et expose un tableau de bord utilisable en conditions quasi réelles.

> ⚠️ **Note pédagogique sur l'ordre des étapes** : L'atelier démarre le simulateur Python **avant** de configurer la destination Eventhouse. Ce choix est intentionnel — Fabric a besoin de données réelles qui circulent pour valider le schéma JSON dans le wizard de configuration. Si vous configurez la destination sans données, le bouton **Finish** reste grisé.

---

## Fichiers nécessaires

- `wind_turbine_simulator.py` — script Python fourni en Partie 2.3
- Package Python : `azure-eventhub`

## Prérequis

- Tenant Microsoft Fabric avec **Real-Time Intelligence** activé
- Licence **Trial** ou **Capacity** compatible Fabric
- Un **workspace Fabric** existant
- **Python 3.9+** installé sur votre poste
- Accès Internet pour envoyer les événements depuis le simulateur Python vers l'endpoint généré par Fabric

## Références officielles utiles

- **Eventstreams overview** : [https://learn.microsoft.com/en-us/fabric/real-time-intelligence/event-streams/overview](https://learn.microsoft.com/en-us/fabric/real-time-intelligence/event-streams/overview)
- **Formats supportés par Eventstream** : [https://learn.microsoft.com/en-us/fabric/real-time-intelligence/event-streams/data-formats](https://learn.microsoft.com/en-us/fabric/real-time-intelligence/event-streams/data-formats)
- **Real-Time Dashboard overview** : [https://learn.microsoft.com/en-us/fabric/real-time-intelligence/real-time-dashboards-overview](https://learn.microsoft.com/en-us/fabric/real-time-intelligence/real-time-dashboards-overview)
- **Activator documentation** : [https://learn.microsoft.com/en-us/fabric/real-time-intelligence/data-activator/](https://learn.microsoft.com/en-us/fabric/real-time-intelligence/data-activator/)
- **Materialized views dans Fabric** : [https://learn.microsoft.com/en-us/fabric/real-time-intelligence/materialized-view](https://learn.microsoft.com/en-us/fabric/real-time-intelligence/materialized-view)

> 💡 **Astuce** : Les écrans Fabric évoluent légèrement au fil des mises à jour. Si un libellé diffère un peu, cherchez l'équivalent le plus proche dans **Real-Time Intelligence**, **Eventstream**, **Eventhouse**, **KQL Database** ou **Dashboard**.

⚠️ **Piège** : Vérifiez que vous travaillez toujours dans le **même workspace**. La majorité des erreurs de début d'atelier viennent d'un Eventstream créé dans un workspace et d'un Eventhouse créé dans un autre.

---

## Partie 1 — Créer l'Eventhouse

### 1.1 — Créer l'item Eventhouse

1. Ouvrir **app.fabric.microsoft.com**
2. Dans la barre latérale gauche, cliquer sur votre **workspace**
3. En haut de la page du workspace, cliquer sur **+ New item**
4. Dans la zone de recherche, taper **Eventhouse**
5. Sélectionner **Eventhouse**
6. Nom : **`WindFarmEventhouse`**
7. Cliquer **Create**

**Ce que vous devez voir à l'écran** :

- Une page Eventhouse qui s'ouvre
- Dans le panneau gauche, une section **KQL databases**
- Une base **`WindFarmEventhouse`** déjà créée automatiquement
- Une zone centrale d'exploration de données

### 1.2 — Vérifier la base KQL

1. Dans le panneau gauche de l'Eventhouse, développer **KQL databases**
2. Cliquer sur **WindFarmEventhouse**
3. Vérifier que les sections **Tables**, **Functions**, **Materialized views** et **Shortcuts** sont visibles

**Ce que vous devez voir à l'écran** :

- L'explorateur KQL de la base
- Un éditeur de requêtes ou une zone d'exploration
- Pour l'instant, aucune table métier créée

> 💡 **Astuce** : Si vous préparez un atelier en direct, laissez cette page ouverte dans un onglet. Vous y reviendrez souvent pour vérifier l'arrivée des événements.

---

## Partie 2 — Créer l'Eventstream et démarrer le simulateur

### 2.1 — Créer le flux et le Custom Endpoint

1. Retourner dans votre **workspace**
2. Cliquer **+ New item**
3. Rechercher **Eventstream**
4. Sélectionner **Eventstream**
5. Nom : **`WindTurbineStream`**
6. Cliquer **Create**
7. Sur le canvas, repérer la zone de départ du flux
8. Cliquer **Use custom endpoint**
9. Nom du endpoint : **`WindTurbineStream-Endpoint`**
10. Cliquer **Add**
11. Vérifier qu'un nœud source apparaît sur la partie gauche du canvas
12. Cliquer **Publish** en haut à droite

**Ce que vous devez voir à l'écran** :

- Un canvas Eventstream avec un nœud source à gauche
- Un bloc central du type **Transform events or add destination**
- Après publication, le stream devient en mode **Live**

> 💡 **Note** : Le **custom endpoint** fournit un point d'entrée compatible pour envoyer les événements depuis votre script Python.

> 💡 **Note** : Publiez tôt — sans publication, les clés de connexion ne sont pas finalisées et votre script Python échouera.

⚠️ **Piège** : Le bouton **Publish** est obligatoire. Vérifiez toujours l'état **Published** avant de passer à la suite.

### 2.2 — Récupérer les clés de connexion

> 💡 **Pourquoi maintenant ?** Nous récupérons les clés immédiatement après la publication, **avant** de configurer la destination Eventhouse. Cela nous permet de lancer le simulateur Python en Partie 2.3, ce qui est indispensable pour que le wizard de configuration de la destination détecte automatiquement le schéma JSON.

1. Passer en mode **Live** si ce n'est pas déjà le cas
2. Cliquer sur le nœud **WindTurbineStream-Endpoint**
3. En bas de l'écran, repérer le panneau **Details**
4. Dans la colonne du panneau, cliquer **SAS Key Authentication**
5. Copier ces 2 valeurs dans un bloc-notes

| Valeur | Où la trouver |
| --- | --- |
| **Event hub name** | Affiché en clair dans le panneau |
| **Connection string-primary key** | Cliquer sur l'icône 👁️ pour révéler, puis copier |

**Ce que vous devez voir à l'écran** :

- Une chaîne de connexion longue commençant par `Endpoint=sb://...`
- Un nom d'Event Hub de type `es_xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`

> 💡 **Astuce** : Copiez les deux valeurs immédiatement dans un bloc-notes sans les modifier. Vous les collerez dans le script Python à l'étape suivante.

### 2.3 — Préparer et lancer le simulateur Python

> 💡 **Pourquoi maintenant ?** Le simulateur doit tourner **avant** la configuration de la destination Eventhouse. Fabric a besoin de données réelles qui circulent dans le stream pour détecter le format JSON et activer le bouton **Finish** dans le wizard de configuration.

#### Installer le package

Ouvrir un **terminal** (PowerShell ou CMD) :

```bash
pip install azure-eventhub
```

**Ce que vous devez voir à l'écran** :

- Le téléchargement du package
- Un message de fin du type `Successfully installed azure-eventhub ...`

> 💡 **Astuce** : Si `pip` n'est pas reconnu, essayez `py -m pip install azure-eventhub`.

⚠️ **Piège** : Installez le package dans le même environnement Python que celui utilisé pour exécuter le script.

#### Créer le script

Créer un fichier **`wind_turbine_simulator.py`** et coller ce code :

```python
import json
import time
import random
from datetime import datetime, timezone
from azure.eventhub import EventHubProducerClient, EventData

# ============================================================
# CONFIGURATION — Coller vos valeurs depuis l'Eventstream
# ============================================================
CONNECTION_STR = "<votre_connection_string_primary_key>"
EVENTHUB_NAME = "<votre_event_hub_name>"
# ============================================================

TURBINES = ["WT-001", "WT-002", "WT-003", "WT-004", "WT-005"]
PARK_NAME = "Parc Eolien Bizerte"

def generate_turbine_event(turbine_id):
    wind_speed = random.uniform(3.0, 25.0)

    # Courbe de puissance simplifiee
    if wind_speed < 3.5:
        power = 0
    elif wind_speed > 22:
        power = 0
    else:
        power = min(3000, (wind_speed - 3.5) ** 2 * 18)

    rotor_rpm = wind_speed * 1.2 + random.uniform(-1, 1)
    gen_temp = 55 + (power / 3000) * 35 + random.uniform(-3, 3)
    vibration = 2.0 + random.uniform(0, 1.5)

    # Injection d'anomalies (10% du temps)
    # Option atelier intensif :
    # Pour observer plus vite les anomalies pendant une demo courte,
    # vous pouvez temporairement remplacer 0.10 par 0.25 ou 0.30.
    # Revenez ensuite a 0.10 pour garder un flux plus realiste.
    if random.random() < 0.10:
        anomaly_type = random.choice(["overheat", "vibration", "underperform"])
        if anomaly_type == "overheat":
            gen_temp += random.uniform(25, 45)
        elif anomaly_type == "vibration":
            vibration += random.uniform(5, 12)
        elif anomaly_type == "underperform":
            power *= random.uniform(0.2, 0.5)

    return {
        "TurbineId": turbine_id,
        "Timestamp": datetime.now(timezone.utc).isoformat(),
        "WindSpeedMs": round(wind_speed, 2),
        "RotorRPM": round(rotor_rpm, 2),
        "PowerOutputKW": round(power, 2),
        "GeneratorTempC": round(gen_temp, 2),
        "VibrationMmS": round(vibration, 2),
        "NacelleOrientation": round(random.uniform(0, 360), 1),
        "AmbientTempC": round(random.uniform(15, 35), 1),
        "ParkName": PARK_NAME
    }

def main():
    producer = EventHubProducerClient.from_connection_string(
        conn_str=CONNECTION_STR,
        eventhub_name=EVENTHUB_NAME
    )
    print("Envoi d'evenements vers Fabric Eventstream...")

    try:
        while True:
            batch = producer.create_batch()
            for turbine_id in TURBINES:
                event = generate_turbine_event(turbine_id)
                batch.add(EventData(json.dumps(event)))
                print(f"  [{event['Timestamp'][:19]}] {turbine_id} | "
                      f"Vent: {event['WindSpeedMs']}m/s | "
                      f"Puissance: {event['PowerOutputKW']}kW | "
                      f"Temp: {event['GeneratorTempC']}C | "
                      f"Vibration: {event['VibrationMmS']}mm/s")
            producer.send_batch(batch)
            print(f"  -> Batch de {len(TURBINES)} evenements envoye\n")
            time.sleep(5)
    except KeyboardInterrupt:
        print("Arret du simulateur.")
    finally:
        producer.close()

if __name__ == "__main__":
    main()
```

#### Configurer le script

1. Ouvrir le fichier dans un éditeur de texte
2. Remplacer `<votre_connection_string_primary_key>` par la **Connection string** copiée en 2.2
3. Remplacer `<votre_event_hub_name>` par le **Event hub name** copié en 2.2
4. Sauvegarder le fichier

⚠️ **Piège** : Ne laissez pas d'espaces ou de guillemets supplémentaires autour des valeurs collées.

#### Lancer le simulateur

Dans le terminal, naviguer vers le dossier où se trouve le script :

```bash
cd <dossier_ou_se_trouve_le_script>
py wind_turbine_simulator.py
```

Si `py` n'existe pas sur votre poste, utilisez `python wind_turbine_simulator.py`.

**Output attendu :**

```text
Envoi d'evenements vers Fabric Eventstream...
  [2026-02-10T17:04:26] WT-001 | Vent: 13.16m/s | Puissance: 1678.44kW | Temp: 77.45C | Vibration: 2.7mm/s
  [2026-02-10T17:04:26] WT-002 | Vent: 23.59m/s | Puissance: 0kW | Temp: 53.19C | Vibration: 2.52mm/s
  ...
  -> Batch de 5 evenements envoye
```

**Ce que vous devez voir à l'écran** :

- Une ligne par turbine toutes les 5 secondes
- Des valeurs qui changent à chaque cycle
- Par moments, des températures très hautes ou des vibrations anormales

> 💡 **Astuce** : Attendez **au moins 2 cycles complets** (10 secondes) avant de passer à l'étape suivante. Fabric a besoin de quelques événements pour détecter le schéma automatiquement.

> 💡 **Astuce** : Laissez ce terminal ouvert pendant tout l'atelier. Utilisez **Ctrl+C** uniquement en fin d'atelier.

⚠️ **Piège** : Si le script démarre puis s'arrête immédiatement, vérifiez la connection string, le nom de l'Event Hub et votre accès Internet.

### 2.4 — Ajouter la destination Eventhouse

> 💡 **Pourquoi Direct ingestion ?** Le formulaire "Event processing before ingestion" de Fabric présente un bug d'interface qui rend le bouton Save systématiquement inaccessible, quelle que soit la manipulation tentée. Nous utilisons donc **Direct ingestion** comme chemin principal — c'est une approche officiellement documentée par Microsoft, et la configuration de la table se fait ensuite dans un wizard séparé qui fonctionne parfaitement dès lors que le simulateur envoie déjà des données.

**Étape 1 — Créer la connexion au nœud Eventhouse**

1. Retourner dans l'Eventstream **WindTurbineStream**
2. Cliquer **Edit** dans la barre supérieure si nécessaire
3. Sur le canvas, cliquer le bloc vert **Transform events or add destination**
4. Sélectionner **Eventhouse**
5. Dans le panneau de droite, sélectionner **Direct ingestion**
6. Remplir uniquement ces trois champs :

| Champ | Valeur |
| --- | --- |
| **Destination name** | `TurbineEventsDestination` |
| **Workspace** | Votre workspace |
| **Eventhouse** | `WindFarmEventhouse` |

7. Cliquer **Save** — le bouton est actif car il n'y a pas de champ table à valider
8. Vérifier qu'un nœud Eventhouse apparaît sur la droite du canvas
9. Cliquer **Publish** en haut à droite
10. Attendre la confirmation puis passer en mode **Live**

**Ce que vous devez voir à l'écran** :

- Un nœud Eventhouse connecté à droite du canvas
- Le statut **Published** confirmé
- Aucun message d'erreur rouge

**Étape 2 — Configurer la table via le wizard Get data**

> 💡 **Rappel** : Le simulateur Python tourne depuis l'étape 2.3. Les données circulent déjà — c'est ce qui permet au wizard de détecter automatiquement le schéma JSON et d'activer le bouton Finish.

1. En mode **Live**, cliquer sur le nœud **Eventhouse** sur le canvas
  
2. Cliquer **Configure**
  
3. L'assistant **Get data** s'ouvre — choisir la KQL Database **`WindFarmEventhouse`**
  
4. Cliquer **New table**
  
5. Saisir le nom de table : **`TurbineEvents`**
  
6. Conserver le nom de connexion proposé
  
7. Cliquer **Next**
  
8. Dans l'étape d'inspection, vérifier que le format détecté est **JSON**
  
  > Le format est détecté automatiquement car le simulateur envoie déjà des événements JSON. Le bouton **Finish** est actif.
  
  > Si le format n'est pas détecté : cliquer sur le sélecteur de format et choisir manuellement **JSON**.
  
9. Cliquer **Finish**
  
10. Dans l'écran de résumé, cliquer **Close**
  

**Ce que vous devez voir à l'écran** :

- L'assistant Get data s'est complété avec succès
- La table `TurbineEvents` est créée dans l'Eventhouse
- Les premières lignes de données commencent à arriver

⚠️ **Piège** : En Direct ingestion, les transformations de flux (colonnes calculées, filtres) ne s'appliquent pas avant l'écriture dans la table. La Partie 2.6 sur les transformations reste dans l'atelier comme extension optionnelle pour les profils qui souhaitent aller plus loin.

### 2.5 — Vérifier la structure du flux

1. Sur le canvas, vérifier que vous avez bien :
  - Un nœud **source custom endpoint** à gauche
  - Un chemin de traitement au centre
  - Une destination **Eventhouse** à droite
2. En haut à droite, vérifier que le flux est bien **Published**

**Ce que vous devez voir à l'écran** :

- Un flux lisible de gauche à droite
- Aucun message d'erreur rouge sur les nœuds
- Une vue stable, qui reçoit les événements du simulateur

> 💡 **Astuce** : Prenez une capture d'écran du canvas. Elle vous servira de référence si vous devez expliquer l'architecture.

---

## Partie 2.6 — Transformations Eventstream (extension optionnelle — profils avancés)

> ⚠️ **Cette partie ne fait pas partie du parcours principal de l'atelier.** En mode **Direct ingestion**, les transformations dans l'Eventstream n'ont aucun effet sur les données écrites dans la table — elles sont ignorées. Les colonnes calculées de diagnostic seront produites directement en KQL dans la Partie 4, avec le même résultat analytique.
> 
> **→ Passez directement à la Partie 3.**

Pour information, si vous souhaitez explorer les transformations Eventstream dans un autre contexte : dans la barre d'outils supérieure du canvas Eventstream, le bouton **"Transform events"** donne accès aux opérateurs disponibles (Manage fields, Filter, Aggregate, etc.). Ces transformations ne sont efficaces qu'en mode **Event processing before ingestion**.

---

## Partie 3 — Vérifier l'ingestion des données

### 3.1 — Vérifier que la table a été créée

1. Retourner sur **app.fabric.microsoft.com**
2. Ouvrir **WindFarmEventhouse**
3. Dans le panneau gauche, développer **KQL databases**
4. Cliquer sur **WindFarmEventhouse**
5. Sous **Tables**, vérifier que **TurbineEvents** apparaît

**Ce que vous devez voir à l'écran** :

- Une table `TurbineEvents`
- Un explorateur qui se remplit automatiquement

> 💡 **Astuce** : Si la table n'apparaît pas immédiatement, attendez 20 à 60 secondes puis actualisez la vue.

⚠️ **Piège** : Si le flux n'était pas publié ou si le script n'envoie rien, la table peut ne pas être créée tout de suite.

### 3.2 — Prévisualiser les données

1. Cliquer sur **TurbineEvents**
2. Ouvrir l'onglet **Data preview**
3. Attendre quelques secondes si nécessaire

**Résultat attendu** : un tableau avec les colonnes :

- `TurbineId`, `Timestamp`, `WindSpeedMs`, `RotorRPM`, `PowerOutputKW`, `GeneratorTempC`, `VibrationMmS`, `NacelleOrientation`, `AmbientTempC`, `ParkName`
- Plus les colonnes enrichies éventuelles : `TempCategory`, `VibrationCategory`, `PowerBand`, `IngestionLabel`, `SiteRegion`, `MaintenanceZone`, `DataSourceType`, `AlertScore`

> 💡 **Astuce** : Si vous voyez déjà les colonnes enrichies dans l'aperçu, cela confirme que les transformations Eventstream ont bien été appliquées avant l'ingestion.

⚠️ **Piège** : Fabric peut parfois ajouter un espace à la fin du nom de la table lors d'une création automatique. Si une requête KQL échoue avec `Failed to resolve table 'TurbineEvents'`, utilisez **`TurbineEvents`**.

⚠️ **Piège** : Selon l'inférence automatique du schéma, la colonne **`Timestamp`** peut être interprétée comme du **texte**. Les requêtes ci-dessous convertissent explicitement `Timestamp` avec `todatetime(Timestamp)`.

### 3.3 — Vérifier avec une requête simple

1. Ouvrir le queryset de la base ou la zone de requête KQL
2. Coller :

```kql
TurbineEvents
| take 10
```

3. Cliquer **Run**

**Ce que vous devez voir à l'écran** :

- Une grille de résultats avec 10 lignes
- Des colonnes identiques à celles observées dans l'aperçu

> 💡 **Astuce** : Si votre table n'a pas d'espace final, remplacez `TurbineEvents` par `TurbineEvents`.

---

## Partie 4 — Requêtes analytiques KQL

1. Dans le panneau gauche, cliquer sur le **queryset** lié à la base
2. Supprimer le contenu par défaut si besoin
3. Copier-coller et exécuter chaque requête avec **Run**

> 💡 **Astuce** : Sélectionnez uniquement la requête à exécuter avant de cliquer **Run**.

> 💡 **Astuce** : Laissez tourner le simulateur pendant toute cette partie pour voir les résultats évoluer.

⚠️ **Piège** : Si vous travaillez dans un autre queryset ou une autre base que `WindFarmEventhouse`, les requêtes peuvent sembler correctes mais ne renverront rien.

---

### 4.1 — Vérification rapide

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| take 20
```

- **`extend`** : Crée une nouvelle colonne `EventTime` en convertissant `Timestamp` (texte → date/heure).
  
- **`take 20`** : Renvoie 20 lignes arbitraires pour un aperçu immédiat.
  

**Ce que vous voyez** : les derniers événements ingérés, avec les colonnes de télémétrie brutes et éventuellement les colonnes enrichies.

---

### 4.2 — Production moyenne par turbine (10 dernières minutes)

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(10m)
| summarize
    AvgPower = avg(PowerOutputKW),
    AvgWind = avg(WindSpeedMs),
    EventCount = count()
  by TurbineId
| order by AvgPower desc
```

- **`where EventTime > ago(10m)`** : Filtre les 10 dernières minutes.
  
- **`summarize ... by TurbineId`** : Groupe par turbine et calcule moyenne/occurrences.
  
- **`order by AvgPower desc`** : Classe de la plus productive à la moins productive.
  

**Ce que vous voyez** : chaque turbine avec sa puissance moyenne, sa vitesse de vent moyenne et son nombre d'événements récents.

---

### 4.3 — Détection d'anomalies thermiques (> 95°C)

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(30m)
| where GeneratorTempC > 95
| project EventTime, TurbineId, GeneratorTempC, PowerOutputKW, WindSpeedMs
| order by GeneratorTempC desc
```

- **`where GeneratorTempC > 95`** : Seuil critique au-delà du risque de casse.
  
- **`project`** : Ne conserve que les colonnes utiles pour le diagnostic.
  
- **`order by GeneratorTempC desc`** : Les plus chaudes d'abord.
  

**Ce que vous voyez** : uniquement les événements où le générateur a dépassé 95°C.

---

### 4.4 — Détection de vibrations anormales (> 8 mm/s)

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(30m)
| where VibrationMmS > 8.0
| project EventTime, TurbineId, VibrationMmS, RotorRPM, GeneratorTempC
| order by VibrationMmS desc
```

- **`where VibrationMmS > 8.0`** : Seuil de vibration dangereuse.
  
- Idem 4.3, mais appliqué aux vibrations.
  

**Ce que vous voyez** : les événements avec des vibrations dangereuses.

---

### 4.5 — Rendement énergétique par turbine (fenêtre de 5 min)

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(30m)
| where WindSpeedMs between (4.0 .. 20.0)
| summarize
    AvgPower = avg(PowerOutputKW),
    AvgWind = avg(WindSpeedMs)
  by TurbineId, bin(EventTime, 5m)
| extend EfficiencyRatio = AvgPower / (AvgWind * AvgWind * AvgWind) * 1000
| order by EventTime desc, TurbineId
```

- **`bin(EventTime, 5m)`** : Regroupe les événements par tranches de 5 minutes.
  
- **`extend EfficiencyRatio = ... / (AvgWind³) * 1000`** : Rendement simplifié (puissance / vent³).
  
- Utile pour comparer l'efficacité entre turbines à vent égal.
  

**Ce que vous voyez** : un ratio de rendement simplifié par turbine toutes les 5 minutes.

---

### 4.6 — État de santé global des turbines

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(30m)
| summarize
    AvgTemp = avg(GeneratorTempC),
    MaxTemp = max(GeneratorTempC),
    AvgVibration = avg(VibrationMmS),
    MaxVibration = max(VibrationMmS),
    AvgPower = avg(PowerOutputKW),
    TotalEvents = count()
  by TurbineId
| extend HealthStatus = case(
    MaxTemp > 95 or MaxVibration > 8.0, "CRITIQUE",
    MaxTemp > 85 or MaxVibration > 6.0, "ATTENTION",
    "NORMAL")
| order by HealthStatus asc, TurbineId asc
```

- **`case(...)`** : Attribue un statut `CRITIQUE` / `ATTENTION` / `NORMAL`.
  
- Synthèse par turbine sur température max, vibration max, puissance moyenne.
  
- **`order by HealthStatus asc`** : Remonte d'abord les plus critiques.
  

**Ce que vous voyez** : un tableau de synthèse avec un statut **CRITIQUE**, **ATTENTION** ou **NORMAL** par turbine.

---

### 4.7 — Courbe glissante de température (granularité 1 minute)

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(1h)
| summarize AvgTemp = avg(GeneratorTempC) by TurbineId, bin(EventTime, 1m)
| order by EventTime asc
```

- **`bin(EventTime, 1m)`** : Agrège par minute.
  
- Idéal pour un **graphique en ligne** où on suit l'évolution minute par minute.
  

**Ce que vous voyez** : une série temporelle idéale pour un graphique en ligne.

---

### 4.8 — Détection de sous-performance par comparaison puissance/vent

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(30m)
| where WindSpeedMs between (6.0 .. 20.0)
| extend ExpectedPowerKW = min_of(3000.0, pow(WindSpeedMs - 3.5, 2) * 18.0)
| extend PerformanceRatio = iif(ExpectedPowerKW == 0, real(null), PowerOutputKW / ExpectedPowerKW)
| where PerformanceRatio < 0.60
| project EventTime, TurbineId, WindSpeedMs, PowerOutputKW, ExpectedPowerKW, PerformanceRatio
| order by PerformanceRatio asc
```

- **`pow(WindSpeedMs - 3.5, 2) * 18.0`** : Puissance théorique attendue.
  
- **`PerformanceRatio < 0.60`** : La turbine produit moins de 60% de l'attendu → sous-performance.
  

**Ce que vous voyez** : les cas où la puissance réellement produite est très en dessous de la puissance attendue.

---

### 4.9 — Carte de chaleur simple de la température

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(1h)
| summarize AvgTemp = avg(GeneratorTempC) by TurbineId, TimeSlot = bin(EventTime, 5m)
| order by TimeSlot asc, TurbineId asc
```

- **`summarize AvgTemp by TurbineId, bin(EventTime, 5m)`** : Préparé pour heatmap.
  
- Chaque case = température moyenne d'une turbine sur 5 minutes.
  

**Ce que vous voyez** : un tableau prêt à être affiché en **heatmap** dans un dashboard.

---

### 4.10 — Jointure avec une petite table de référence

```kql
let TurbineReference = datatable(TurbineId:string, SiteArea:string, NominalPowerKW:real, CommissionYear:int)
[
    "WT-001", "Nord-Ouest", 3000.0, 2021,
    "WT-002", "Nord-Ouest", 3000.0, 2021,
    "WT-003", "Centre", 3000.0, 2022,
    "WT-004", "Centre", 3000.0, 2022,
    "WT-005", "Sud", 3000.0, 2023
];
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(15m)
| summarize AvgPower = avg(PowerOutputKW), MaxTemp = max(GeneratorTempC) by TurbineId
| join kind=leftouter TurbineReference on TurbineId
| extend LoadPct = round((AvgPower / NominalPowerKW) * 100.0, 2)
| project TurbineId, SiteArea, CommissionYear, AvgPower, LoadPct, MaxTemp
| order by LoadPct desc
```

- **`let TurbineReference = datatable(...)`** : Table temporaire (site, année, puissance nominale).
  
- **`join kind=leftouter`** : Enrichit les données de télémétrie avec des métadonnées.
  
- **`LoadPct = round(AvgPower / NominalPowerKW * 100, 2)`** : Charge relative.
  

**Ce que vous voyez** : une analyse enrichie par des métadonnées de référence.

---

### 4.11 — Top 3 anomalies par score combiné

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(30m)
| extend ExpectedPowerKW = min_of(3000.0, pow(WindSpeedMs - 3.5, 2) * 18.0)
| extend UnderPerfPenalty = iif(WindSpeedMs >= 8 and PowerOutputKW < ExpectedPowerKW * 0.6, 25.0, 0.0)
| extend TempPenalty = iif(GeneratorTempC > 95, 50.0, iif(GeneratorTempC > 85, 20.0, 0.0))
| extend VibPenalty = iif(VibrationMmS > 8, 40.0, iif(VibrationMmS > 6, 15.0, 0.0))
| extend AnomalyScore = TempPenalty + VibPenalty + UnderPerfPenalty
| where AnomalyScore > 0
| top 3 by AnomalyScore desc
| project EventTime, TurbineId, GeneratorTempC, VibrationMmS, PowerOutputKW, WindSpeedMs, AnomalyScore
```

- **Trois pénalités additionnées** : sous-performance (25), surchauffe (20/50), vibration (15/40).
  
- **`top 3 by AnomalyScore desc`** : Événements les plus graves en premier.
  

**Ce que vous voyez** : les 3 événements les plus préoccupants, classés selon un score combiné.

---

### 4.12 — Tendance récente de production par comparaison entre deux fenêtres

```kql
let CurrentWindow =
    TurbineEvents
    | extend EventTime = todatetime(Timestamp)
    | where EventTime between (ago(10m) .. now())
    | summarize CurrentAvgPower = avg(PowerOutputKW) by TurbineId;
let PreviousWindow =
    TurbineEvents
    | extend EventTime = todatetime(Timestamp)
    | where EventTime between (ago(20m) .. ago(10m))
    | summarize PreviousAvgPower = avg(PowerOutputKW) by TurbineId;
CurrentWindow
| join kind=inner PreviousWindow on TurbineId
| extend DeltaPower = CurrentAvgPower - PreviousAvgPower
| extend Trend = case(DeltaPower > 150, "HAUSSE", DeltaPower < -150, "BAISSE", "STABLE")
| project TurbineId, PreviousAvgPower, CurrentAvgPower, DeltaPower, Trend
| order by DeltaPower asc
```

- **Deux sous-requêtes** : fenêtre courante (10m) vs fenêtre précédente (20m à 10m).
  
- **`join` puis `case(...)`** : Compare les puissances moyennes → `HAUSSE` / `BAISSE` / `STABLE`.
  

**Ce que vous voyez** : si chaque turbine est en hausse, en baisse ou stable par rapport à la fenêtre précédente.

---

## Partie 5 — Créer le Real-Time Dashboard

Vous allez maintenant créer un dashboard opérationnel avec **au moins 6 tuiles**.

### 5.1 — Première tuile : tableau de santé

1. Dans le queryset, sélectionner la **requête 4.6**
2. Cliquer **Save to Dashboard**
3. Sélectionner **Create new dashboard**
4. Nom du dashboard : **`WindFarm-LiveMonitoring`**
5. Nom de la tuile : **`Etat de sante des turbines`**
6. Cliquer **Create**
7. Cliquer **Open dashboard**

**Résultat attendu** : un tableau avec les 5 turbines et leur statut de santé.

> 💡 **Astuce** : Il est possible d'atteindre le dashboard via la barre de recherches en haut au centre s'il est fermé

### 5.2 — Deuxième tuile : graphique température

1. Vérifier que vous êtes dans l'onglet **Home** du dashboard
2. Cliquer **New tile**
3. Connecter à la **database** `WindFarmEventhouse` si demandé
4. Coller cette requête :

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(1h)
| summarize MaxTemp = max(GeneratorTempC) by TurbineId, bin(EventTime, 1m)
```

- **`todatetime(Timestamp)`** : Convertit le champ texte en type date/heure pour les filtres temporels.
  
- **`where EventTime > ago(1h)`** : Limite l'analyse à la dernière heure glissante.
  
- **`bin(EventTime, 1m)`** : Regroupe les événements par tranches de 1 minute (arrondi à la minute inférieure).
  
- **`summarize MaxTemp = max(...) by TurbineId, bin(...)`** : Pour chaque turbine et chaque minute, garde la température la plus élevée.
  
- **Utilité** : Idéal pour un graphique en ligne ou une heatmap — on voit les pics de température minute par minute.
  

5. Cliquer **Run**
6. Cliquer **+ Add visual**
7. Dans le panneau de configuration à droite, remplir les champs dans cet ordre :

**Section générale**

| Champ | Valeur |
| --- | --- |
| **Tile name** | `Température max par turbine` |
| **Visual type** | `Line chart` |
| **Y columns** | `MaxTemp` |
| **X column** | `EventTime` |
| **Series columns** | `TurbineId` |

**Section Y Axis** (faire défiler le panneau vers le bas)

| Champ | Valeur |
| --- | --- |
| **Label** | `Température (°C)` |
| **Maximum value** | `Infer` (laisser par défaut) |
| **Minimum value** | `40` |
| **Y axis scale** | `Linear` |

**Section X Axis**

| Champ | Valeur |
| --- | --- |
| **Label** | `Heure` |
| **X axis scale** | `Linear` |

8. Cliquer **Apply changes** en haut à droite

**Résultat attendu** : 5 courbes colorées, une par turbine, avec des pics visibles lors des surchauffes.

⚠️ **Piège** : Si toutes les courbes se superposent de manière illisible, vérifiez que **Series columns** est bien défini sur `TurbineId`.

⚠️ **Piège** : Si le graphique affiche une ligne plate à zéro, vérifiez que **Y columns** est bien `MaxTemp` et non une autre colonne.

### 5.3 — Troisième tuile : vibrations (Area chart)

1. Dans l'onglet **Home**, cliquer **New tile**
2. Coller cette requête :

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(1h)
| summarize MaxVibration = max(VibrationMmS) by TurbineId, bin(EventTime, 1m)
```

- **`todatetime(Timestamp)`** : Conversion du timestamp texte en format date/heure exploitable.
  
- **`where EventTime > ago(1h)`** : Filtre sur la dernière heure glissante uniquement.
  
- **`bin(EventTime, 1m)`** : Arrondit chaque timestamp à la minute inférieure pour agréger par tranche.
  
- **`summarize MaxVibration = max(VibrationMmS) by TurbineId, bin(...)`** : Pour chaque turbine et chaque minute, ne retient que la valeur de vibration la plus élevée.
  
- **Utilité** : Parfait pour détecter les pics de vibration anormaux et les corréler avec d'autres événements (température, vent).
  

3. Cliquer **Run**
4. Cliquer **+ Add visual**
5. Dans le panneau **Visual formatting** à droite, remplir les champs :

**Section générale**

| Champ | Valeur |
| --- | --- |
| **Tile name** | `Vibrations max par turbine` |
| **Visual type** | `Area chart` |

**Section Data**

| Champ | Valeur |
| --- | --- |
| **Y columns** | `MaxVibration` |
| **X column** | `EventTime` |
| **Series columns** | `TurbineId` |

**Section Legend**

| Champ | Valeur |
| --- | --- |
| **Show** | Activé |
| **Legend location** | `Bottom` |

**Section Y Axis**

| Champ | Valeur |
| --- | --- |
| **Label** | `Vibration (mm/s)` |
| **Maximum value** | `Infer` |
| **Minimum value** | `Infer` |
| **Y axis scale** | `Linear` |

6. Cliquer **Apply changes** en haut à droite

**Résultat attendu** : 5 zones colorées superposées, une par turbine, avec des pics lors des anomalies de vibration.

> 💡 **Astuce** : L'Area chart rend les pics de vibration plus visibles que le Line chart car les zones colorées se distinguent bien visuellement. Placez cette tuile à côté de la température pour comparer les deux types d'anomalies.

### 5.4 — Quatrième tuile : production moyenne minute par minute

1. Dans l'onglet **Home**, cliquer **New tile**
2. Coller cette requête :

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(1h)
| summarize AvgPower = avg(PowerOutputKW) by TurbineId, bin(EventTime, 1m)
```

- **`todatetime(Timestamp)`** : Conversion du champ texte en type date/heure pour les opérations temporelles.
  
- **`where EventTime > ago(1h)`** : Restreint l'analyse à la dernière heure de fonctionnement.
  
- **`bin(EventTime, 1m)`** : Regroupe les événements par tranches horaires d'une minute.
  
- **`summarize AvgPower = avg(PowerOutputKW) by TurbineId, bin(...)`** : Pour chaque turbine et chaque minute, calcule la puissance moyenne produite.
  
- **Utilité** : Visualise l'évolution de la production en continu, détecte les baisses de régime ou les arrêts soudains turbine par turbine.
  

3. Cliquer **Run**
4. Cliquer **+ Add visual**
5. Dans le panneau **Visual formatting** à droite, remplir les champs :

**Section générale**

| Champ | Valeur |
| --- | --- |
| **Tile name** | `Production moyenne par turbine` |
| **Visual type** | `Column chart` |

**Section Data** — Fabric peut mal inférer les colonnes, vérifier et corriger si nécessaire

| Champ | Valeur |
| --- | --- |
| **Y columns** | `AvgPower` uniquement |
| **X column** | `EventTime` |
| **Series columns** | `TurbineId` |

**Section Y Axis**

| Champ | Valeur |
| --- | --- |
| **Label** | `Puissance (kW)` |
| **Minimum value** | `0` |
| **Maximum value** | `Infer` |
| **Y axis scale** | `Linear` |

**Section X Axis**

| Champ | Valeur |
| --- | --- |
| **Label** | `Heure` |

6. Cliquer **Apply changes** en haut à droite

**Résultat attendu** : des colonnes groupées par tranche de temps, une couleur par turbine, avec des variations visibles selon le vent.

⚠️ **Piège** : Si Fabric place `EventTime` dans Y columns et `TurbineId` dans X column, corrigez-les manuellement — l'inférence automatique se trompe parfois avec les Column charts.

### 5.5 — Cinquième tuile : KPI global

1. Dans l'onglet **Home**, cliquer **New tile**
2. Coller cette requête :

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(10m)
| summarize TotalAvgPowerKW = round(avg(PowerOutputKW), 2)
```

- **`where EventTime > ago(10m)`** : Filtre sur la fenêtre des 10 dernières minutes seulement.
  
- **`avg(PowerOutputKW)`** : Calcule la moyenne de toutes les turbines confondues sur cette période.
  
- **`round(..., 2)`** : Arrondit le résultat à 2 décimales pour une lecture propre.
  
- **Absence de `by`** : Pas de regroupement par turbine → une valeur unique, globale.
  
- **Utilité** : KPI synthétique pour supervision en salle de contrôle — donne la température globale de la production du parc.
  

3. Cliquer **Run**
4. Cliquer **+ Add visual**, choisir **Card** ou **Stat** selon votre interface
5. Cliquer **Apply changes**

> 💡 **Astuce** : Les tuiles KPI sont très utiles pour un écran de supervision en salle d'exploitation.

### 5.6 — Sixième tuile : carte de chaleur température

1. Dans l'onglet **Home**, cliquer **New tile**
2. Coller cette requête :

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(1h)
| summarize AvgTemp = avg(GeneratorTempC) by TurbineId, TimeSlot = bin(EventTime, 5m)
| order by TimeSlot asc
```

- **`bin(EventTime, 5m)`** : Regroupe par tranches de 5 minutes (plus lisible qu'1 minute pour une heatmap).
  
- **`summarize AvgTemp = avg(...) by TurbineId, TimeSlot = bin(...)`** : Température moyenne de chaque turbine sur chaque tranche de 5 minutes.
  
- **`order by TimeSlot asc`** : Classe les tranches de la plus ancienne à la plus récente.
  
- **Structure idéale pour heatmap** : Lignes = `TurbineId`, colonnes = `TimeSlot`, valeurs = `AvgTemp`.
  
- **Utilité** : Visualisation immédiate des périodes de surchauffe — plus la case est rouge, plus la turbine a chauffé.
  

3. Cliquer **Run**
4. Cliquer **+ Add visual**
5. Dans le panneau **Visual formatting** à droite, remplir les champs :

**Section générale**

| Champ | Valeur |
| --- | --- |
| **Tile name** | `Carte de chaleur température` |
| **Visual type** | `Heatmap` |

**Section Data** — la Heatmap a une structure différente des autres visuels

| Champ | Valeur |
| --- | --- |
| **Y column** | `TurbineId` |
| **X column** | `TimeSlot` |
| **Value column** | `AvgTemp` |

**Section X Axis**

| Champ | Valeur |
| --- | --- |
| **Label** | `Tranche horaire` |

**Section Y Axis**

| Champ | Valeur |
| --- | --- |
| **Label** | `Turbine` |

6. Cliquer **Apply changes** en haut à droite

**Résultat attendu** : une grille avec les turbines en lignes, les tranches de 5 minutes en colonnes, et une intensité de couleur qui monte avec la température.

> 💡 **Astuce** : La Heatmap est le seul visual qui utilise **Y column** + **X column** + **Value column** au lieu de Y columns + Series columns. Ne pas confondre avec les autres tuiles.

⚠️ **Piège** : Si les colonnes sont mal inférées, vérifiez que **Value column** est bien `AvgTemp` — c'est elle qui détermine la couleur de chaque case.

⚠️ **Piège** : Selon la version de l'interface, la heatmap peut être présentée sous un autre nom. Si vous ne la trouvez pas, gardez une table colorée.

### 5.7 — Septième tuile : top anomalies

1. Dans l'onglet **Home**, cliquer **New tile**
2. Coller cette requête :

```kql
TurbineEvents
| extend EventTime = todatetime(Timestamp)
| where EventTime > ago(30m)
| extend ExpectedPowerKW = min_of(3000.0, pow(WindSpeedMs - 3.5, 2) * 18.0)
| extend UnderPerfPenalty = iif(WindSpeedMs >= 8 and PowerOutputKW < ExpectedPowerKW * 0.6, 25.0, 0.0)
| extend TempPenalty = iif(GeneratorTempC > 95, 50.0, iif(GeneratorTempC > 85, 20.0, 0.0))
| extend VibPenalty = iif(VibrationMmS > 8, 40.0, iif(VibrationMmS > 6, 15.0, 0.0))
| extend AnomalyScore = TempPenalty + VibPenalty + UnderPerfPenalty
| where AnomalyScore > 0
| top 10 by AnomalyScore desc
| project EventTime, TurbineId, GeneratorTempC, VibrationMmS, PowerOutputKW, AnomalyScore
```

- **`ExpectedPowerKW = min_of(3000, pow(vitesse - 3.5, 2) * 18)`** : Puissance théorique max selon la vitesse du vent (plafonnée à 3000 kW).
  
- **Trois pénalités additionnées** : Sous-performance (25 pts), surchauffe (20/50 pts selon sévérité), vibration (15/40 pts selon sévérité).
  
- **`AnomalyScore = somme des pénalités`** : Plus le score est élevé, plus l'événement cumule de problèmes.
  
- **`top 10 by AnomalyScore desc`** : Remonte les 10 événements les plus graves en premier.
  
- **Utilité** : Priorisation des interventions — on traite d'abord la turbine avec le score le plus élevé.
  

3. Cliquer **Run**
4. Cliquer **+ Add visual**, choisir **Table**
5. Cliquer **Apply changes**

> 💡 **Astuce** : Cette tuile complète parfaitement le KPI global. Le KPI montre l'état général, la tuile anomalies montre les cas précis à investiguer.

### 5.8 — Activer le rafraîchissement automatique

1. Dans le dashboard, cliquer l'onglet **Manage** (barre du haut)
2. Ouvrir **Auto refresh**
3. Sélectionner **30 secondes** ou la valeur la plus proche disponible
4. Sauvegarder

> 💡 **Astuce** : Si rien ne bouge dans le dashboard, vérifiez d'abord que le simulateur Python tourne encore.

---

## Partie 6 — Pipeline d'alertes industriel : Update Policy + Function + Materialized View

Dans cette partie, vous allez construire un **système d'alerting complet** entièrement dans l'Eventhouse, sans dépendance externe. L'architecture finale est :

`TurbineEvents` → *(Update Policy)* → `fn_DetectAnomaly()` → `TurbineAlerts` → *(Materialized View)* → `MV_AlertSummary` → *(Dashboard)*

> 💡 **Pourquoi cette approche ?** L'Update Policy est un mécanisme natif de l'Eventhouse : à chaque ingestion dans `TurbineEvents`, la fonction de détection s'exécute automatiquement et écrit les anomalies dans `TurbineAlerts`. Pas d'email, pas de Teams, pas de dépendance externe. Les alertes sont immédiatement requêtables et visualisables dans le dashboard.

### 6.1 — Créer la table TurbineAlerts

1. Ouvrir **WindFarmEventhouse_queryset**
2. Coller et exécuter cette commande :

```kql
.create table TurbineAlerts (
    AlertTime: datetime,
    TurbineId: string,
    AlertType: string,
    Severity: string,
    AlertValue: real,
    Threshold: real,
    Message: string
)
```

3. Cliquer **Run**

**Ce que vous devez voir** : un message de succès. La table `TurbineAlerts` apparaît dans le panneau gauche sous **Tables**.

### 6.2 — Créer la fonction de détection fn_DetectAnomaly

La logique de détection des 3 types d'anomalies est centralisée dans une seule fonction réutilisable. L'Update Policy appellera cette fonction — si vous souhaitez ajouter une règle, vous modifiez uniquement la fonction.

1. Coller et exécuter cette commande :

```kql
.create-or-alter function fn_DetectAnomaly() {
    TurbineEvents
    | extend EventTime = todatetime(Timestamp)
    | extend ExpectedPowerKW = min_of(3000.0, pow(WindSpeedMs - 3.5, 2) * 18.0)
    | extend PerformanceRatio = iif(
        ExpectedPowerKW == 0 or WindSpeedMs < 6.0 or WindSpeedMs > 20.0,
        real(null),
        PowerOutputKW / ExpectedPowerKW)
    | where GeneratorTempC > 95
        or VibrationMmS > 8
        or (isnotnull(PerformanceRatio) and PerformanceRatio < 0.60)
    | extend AlertType = case(
        GeneratorTempC > 95, "SURCHAUFFE",
        VibrationMmS > 8,    "VIBRATION",
                             "SOUS_PERFORMANCE")
    | extend AlertValue = case(
        GeneratorTempC > 95, GeneratorTempC,
        VibrationMmS > 8,    VibrationMmS,
                             round(PerformanceRatio * 100, 1))
    | extend Threshold = case(
        GeneratorTempC > 95, 95.0,
        VibrationMmS > 8,    8.0,
                             60.0)
    | extend Severity = case(
        GeneratorTempC > 120 or VibrationMmS > 14, "CRITIQUE",
                                                    "ATTENTION")
    | extend Message = strcat(
        AlertType, " sur ", TurbineId,
        " — Valeur: ", tostring(round(AlertValue, 2)),
        " / Seuil: ", tostring(Threshold))
    | project AlertTime = EventTime, TurbineId, AlertType, Severity, AlertValue, Threshold, Message
}
```

2. Cliquer **Run**

**Ce que vous devez voir** : un message de succès. La fonction `fn_DetectAnomaly` apparaît sous **Functions** dans le panneau gauche.

> 💡 **Astuce** : Testez la fonction seule avant de l'attacher à la policy — exécutez `fn_DetectAnomaly()` dans le queryset et vérifiez que des lignes d'anomalies apparaissent.

### 6.3 — Activer l'Update Policy

1. Coller et exécuter cette commande :

```kql
.alter table TurbineAlerts policy update
@'[{"IsEnabled": true, "Source": "TurbineEvents", "Query": "fn_DetectAnomaly()", "IsTransactional": false, "PropagateIngestionProperties": false}]'
```

2. Cliquer **Run**

**Ce que vous devez voir** : un message de succès confirmant l'activation.

> 💡 **Ce qui se passe maintenant** : à chaque batch ingéré dans `TurbineEvents`, Fabric exécute automatiquement `fn_DetectAnomaly()` et écrit les anomalies dans `TurbineAlerts`. Le pipeline est actif en temps réel.

⚠️ **Piège** : L'Update Policy ne retraite pas les données déjà présentes dans `TurbineEvents`. Elle s'applique uniquement aux nouvelles ingestions. Attendez un cycle du simulateur (5 secondes) avant de vérifier.

### 6.4 — Créer la Materialized View MV_AlertSummary

Pour accélérer le dashboard d'alertes, créez une vue pré-agrégée par turbine, type et heure.

1. Coller et exécuter cette commande :

```kql
.create-or-alter materialized-view with (backfill=true) MV_AlertSummary on table TurbineAlerts
{
    TurbineAlerts
    | summarize
        NbAlertes = count(),
        DerniereAlerte = max(AlertTime)
      by TurbineId, AlertType, Severity, bin(AlertTime, 1h)
}
```

2. Cliquer **Run**

**Ce que vous devez voir** : un message de succès. `MV_AlertSummary` apparaît sous **Materialized views**.

### 6.5 — Requêtes de validation interactives

Attendez un cycle du simulateur puis exécutez ces requêtes dans le queryset pour valider le pipeline :

**Comptage par type et sévérité (30 dernières minutes)**

```kql
TurbineAlerts
| where AlertTime > ago(30m)
| summarize count() by AlertType, Severity
| order by count_ desc
```

**Tendance des alertes par turbine et par tranche de 5 minutes**

```kql
TurbineAlerts
| where AlertTime > ago(1h)
| summarize NbAlertes = count() by TurbineId, AlertType, bin(AlertTime, 5m)
| order by AlertTime desc
```

**Lecture directe de la Materialized View**

```kql
MV_AlertSummary
| where AlertTime > ago(1h)
| order by DerniereAlerte desc
```

> 💡 **Astuce** : Pour générer rapidement des alertes de test, remplacez temporairement `0.10` par `0.50` dans le simulateur Python (taux d'anomalie), relancez le script et attendez 2 cycles.

### 6.6 — Tuile dashboard "Alertes en cours"

1. Dans le dashboard **WindFarm-LiveMonitoring**, onglet **Home**, cliquer **New tile**
2. Coller cette requête :

```kql
TurbineAlerts
| where AlertTime > ago(30m)
| summarize NbAlertes = count(), DerniereAlerte = max(AlertTime) by TurbineId, AlertType, Severity
| order by DerniereAlerte desc
```

3. Cliquer **Run**
4. Cliquer **+ Add visual**
5. Configurer :

| Champ | Valeur |
| --- | --- |
| **Tile name** | `Alertes en cours (30 min)` |
| **Visual type** | `Table` |

6. Cliquer **Apply changes**
7. Dans l'onglet **Manage**, vérifier que **Auto refresh** est à **30 secondes** (configuré en 5.8)

**Résultat attendu** : une tuile tableau qui se met à jour automatiquement, listant les turbines en anomalie avec le type, la sévérité et le nombre d'occurrences récentes.

### 6.7 — Surveiller le pipeline d'alertes

> 💡 **Note** : Microsoft Fabric dispose d'un **Workspace Monitoring** dédié — un Eventhouse séparé qui collecte les logs d'activité du workspace (ingestion, requêtes, performances, erreurs d'Update Policy). Ce volet est traité en détail dans la **Partie 9 — Monitoring du Workspace**.

En attendant, pour vérifier rapidement que l'Update Policy fonctionne, exécutez cette requête dans le queryset :

```kql
TurbineAlerts
| summarize LastAlert = max(AlertTime), Total = count()
| extend PipelineActif = iff(LastAlert > ago(1m), "✅ OK", "⚠️ Vérifier")
```

Si `PipelineActif` affiche `⚠️ Vérifier`, assurez-vous que le simulateur Python tourne et qu'au moins un cycle (5 secondes) s'est écoulé depuis l'activation de l'Update Policy.

---

## Partie 7 — Materialized View sur TurbineEvents

> 💡 **Rappel** : Une `MV_AlertSummary` sur `TurbineAlerts` a déjà été créée en Partie 6.4. Cette partie crée une seconde Materialized View, `mv_turbine_health_1m`, sur la table brute `TurbineEvents`. Les deux vues ont des rôles distincts : `MV_AlertSummary` agrège les alertes, `mv_turbine_health_1m` agrège la télémétrie brute pour accélérer les tuiles du dashboard opérationnel.

Dans un contexte réel, les dashboards exécutent souvent les mêmes agrégations en permanence. Une **Materialized View** pré-calcule les résultats une fois et les met à jour incrémentalement.

### 7.1 — Créer mv_turbine_health_1m

1. Dans **WindFarmEventhouse**, cliquer **+ New** → **Materialized view**
2. Remplacer le contenu par la commande suivante :

```kql
.create-or-alter materialized-view with (backfill=true) mv_turbine_health_1m on table TurbineEvents
{
    TurbineEvents
    | extend EventTime = todatetime(Timestamp)
    | summarize
        AvgPower = avg(PowerOutputKW),
        MaxTemp = max(GeneratorTempC),
        MaxVibration = max(VibrationMmS),
        AvgWind = avg(WindSpeedMs),
        EventCount = count()
      by TurbineId, bin(EventTime, 1m)
}
```

3. Cliquer **Run**

**Ce que vous devez voir** : un message de succès. `mv_turbine_health_1m` apparaît sous **Materialized views**.

> `backfill=true` recalcule immédiatement toutes les données historiques. Les cinq agrégats (puissance, température, vibration, vent, count) sont pré-calculés par turbine et par minute.

### 7.2 — Vérifier la vue

```kql
mv_turbine_health_1m
| where EventTime > ago(30m)
| order by EventTime desc
| take 20
```

**Ce que vous devez voir** : une ligne par turbine et par minute, avec les colonnes `AvgPower`, `MaxTemp`, `MaxVibration`, `AvgWind`, `EventCount`.

### 7.3 — Utiliser la vue dans le dashboard

Remplacez les requêtes coûteuses du dashboard par des requêtes sur la vue :

```kql
mv_turbine_health_1m
| where EventTime > ago(1h)
| project EventTime, TurbineId, MaxTemp
```

```kql
mv_turbine_health_1m
| where EventTime > ago(1h)
| project EventTime, TurbineId, MaxVibration
```

> 💡 Gardez la table brute `TurbineEvents` pour l'exploration détaillée et les deux Materialized Views pour l'affichage opérationnel temps réel.

⚠️ **Piège** : Une Materialized View n'est pas un remplacement total de la table brute. Elle accélère des agrégations répétitives bien identifiées.

---

## Partie 8 — Lecture guidée des résultats

### 8.1 — Ce que vous devez observer côté Eventstream

1. Le canvas est publié
2. Les nœuds source et destination sont reliés
3. Aucun nœud ne présente d'erreur bloquante
4. La destination Eventhouse est active en mode Direct ingestion

### 8.2 — Ce que vous devez observer côté KQL Database

**Tables**

- `TurbineEvents` reçoit régulièrement des lignes (simulateur actif)
- `TurbineAlerts` s'alimente automatiquement via l'Update Policy

**Functions**

- `fn_DetectAnomaly` est visible et peut être exécutée directement pour tester la logique

**Materialized views**

- `mv_turbine_health_1m` agrège la télémétrie par turbine et par minute
- `MV_AlertSummary` agrège les alertes par turbine, type et heure

### 8.3 — Ce que vous devez observer côté Dashboard

1. La tuile de santé affiche les 5 turbines avec leur statut CRITIQUE/ATTENTION/NORMAL
2. Le graphique température montre des pics occasionnels au-dessus de 95°C
3. Le graphique vibrations (Area chart) révèle les pics mécaniques
4. Le Column chart de production varie selon le vent simulé
5. Le KPI global de puissance change à chaque rafraîchissement
6. La heatmap révèle les turbines et tranches horaires les plus chaudes
7. La tuile top anomalies liste les événements les plus critiques par score
8. La tuile **Alertes en cours** liste les turbines en anomalie avec type et sévérité

### 8.4 — Ce que vous devez observer côté Pipeline d'alertes

1. La table `TurbineAlerts` reçoit de nouvelles lignes à chaque cycle du simulateur
2. Les trois types d'alertes sont présents : `SURCHAUFFE`, `VIBRATION`, `SOUS_PERFORMANCE`
3. Les sévérités `CRITIQUE` et `ATTENTION` sont correctement attribuées
4. `MV_AlertSummary` reflète les comptages agrégés par heure
5. Le panneau **Monitoring → Ingestion** de l'Eventhouse ne montre aucune erreur d'Update Policy

> 💡 **Astuce** : Pendant une démonstration, gardez trois onglets ouverts : **simulateur Python**, **dashboard**, **KQL queryset**. C'est la combinaison la plus parlante pour montrer le temps réel.

---

---

## Partie 9 — Monitoring du Workspace

Le **Workspace Monitoring** de Microsoft Fabric est un Eventhouse dédié qui collecte automatiquement les logs d'activité de votre workspace : ingestion, requêtes, commandes DDL, performances. Il vous permet de diagnostiquer les erreurs d'Update Policy, les goulets d'ingestion et les requêtes lentes directement en KQL.

> ⚠️ **Prérequis** : Vous devez avoir le rôle **Admin** sur le workspace pour activer cette fonctionnalité.

### 9.1 — Activer le Workspace Monitoring

**Chemin principal — Workspace Settings :**

1. Dans votre workspace, cliquer sur l'icône **engrenage ⚙️** en haut à droite → **Workspace settings**
2. Dans le panneau gauche, cliquer **Monitoring**
3. Cliquer le bouton **+ Eventhouse**
4. Attendre quelques secondes

**Ce que vous voyez à l'écran** :

- Message de confirmation : *"Successfully added the monitoring Eventhouse and turned on logging"*
- **Log workspace activity : On**
- Un lien cliquable **Monitoring database** apparaît dans ce même panneau

> 💡 **Accès alternatif** : depuis l'item **WindFarmEventhouse** dans le workspace, dans le panneau de navigation gauche, un lien **Monitoring** permet d'accéder directement à la base de monitoring.

---

### 9.2 — Accéder à la base Monitoring

**Option A — Via le lien dans Workspace Settings :**

1. Rester dans **Workspace settings → Monitoring**
2. Cliquer le lien **Monitoring database**

**Option B — Via l'Eventhouse :**

1. Depuis le workspace, ouvrir **WindFarmEventhouse**
2. Dans le panneau gauche, cliquer **Monitoring**

Dans les deux cas, vous atterrissez dans une KQL Database dédiée, distincte de **WindFarmDB**.

---

### 9.3 — Découvrir les tables disponibles

Avant d'écrire des requêtes, explorez les tables présentes dans la base Monitoring :

```kql
.show tables
```

Les principales tables pertinentes pour cet atelier :

| Table | Contenu |
| --- | --- |
| `EventhouseDataOperationLogs` | Toutes les opérations de données : ingestion, requêtes, commandes — filtrées par `DataOperationKind` |

Les colonnes clés de `EventhouseDataOperationLogs` :

| Colonne | Type | Description |
| --- | --- | --- |
| `Timestamp` | datetime | Horodatage de l'opération |
| `OperationName` | string | Nom de l'opération |
| `DatabaseName` | string | Base de données cible |
| `TableName` | string | Table cible (ingestion) |
| `DataOperationKind` | string | Type d'opération : `Ingest`, `Query`, `Command`, etc. |
| `Status` | string | Résultat : `Succeeded`, `Failed`, `PartiallySucceeded` |
| `DurationMs` | long | Durée en millisecondes |
| `TotalRowsCount` | long | Nombre de lignes ingérées |
| `OriginalSizeInBytes` | long | Taille des données reçues |
| `CpuTimeMs` | long | CPU consommé |
| `ItemName` | string | Nom de l'Eventhouse source |

> 💡 Ces tables sont en **lecture seule**. Pour voir le schéma complet : `.show table EventhouseDataOperationLogs schema`

---

## Nettoyage (optionnel)

### 1 — Arrêter l'envoi des événements

1. Revenir dans le terminal
2. Appuyer sur **Ctrl+C**
3. Vérifier le message : `Arret du simulateur.`

### 2 — Supprimer le pipeline d'alertes

1. Dans le queryset, exécuter ces deux commandes :

```kql
.drop table TurbineAlerts
```

```kql
.drop function fn_DetectAnomaly
```

2. Sous **Materialized views**, supprimer **MV_AlertSummary** via le menu **...**

### 3 — Supprimer les tuiles ou le dashboard

1. Ouvrir le dashboard **WindFarm-LiveMonitoring**
2. Cliquer **Manage**
3. Supprimer les tuiles ou le dashboard complet

### 4 — Supprimer les Materialized Views de télémétrie

1. Dans la base **WindFarmEventhouse**, sous **Materialized views**, repérer **mv_turbine_health_1m**
2. Ouvrir le menu **...**
3. Cliquer **Delete**

### 5 — Supprimer l'Eventstream

1. Revenir au **workspace**
2. Sur **WindTurbineStream**, cliquer **...** → **Delete** → Confirmer

### 6 — Supprimer l'Eventhouse

1. Sur **WindFarmEventhouse**, cliquer **...** → **Delete** → Confirmer

> ⚠️ La suppression de l'Eventhouse supprime également toutes les tables, fonctions et Materialized Views qu'il contient.

### 7 — Supprimer éventuellement tout le workspace de laboratoire

1. Ouvrir **Workspace settings** → **General** → **Remove this workspace**
